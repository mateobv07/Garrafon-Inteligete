import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    container: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center'
    },
    pagesContainer: {
        marginLeft: '240px',
        marginTop: '30px'
    }

}));